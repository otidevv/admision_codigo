@model ADMISION.ENTITIES.Models.Info.FaqItem

@{
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Nueva pregunta frecuente";
}

<section class="fade-up mb-5">
    <div class="flex items-center gap-2 text-xs text-ink-400 mb-2">
        <a href="/admin/info-management" class="hover:text-primary-600 transition-colors">Publicaciones</a>
        <i class="ti ti-chevron-right text-[9px]"></i>
        <a href="/admin/info-management/chatbot" class="hover:text-primary-600 transition-colors">Chatbot · FAQ</a>
        <i class="ti ti-chevron-right text-[9px]"></i>
        <span class="text-ink-700 dark:text-ink-200 font-medium">Nueva</span>
    </div>
    <div class="eyebrow">Portal público</div>
    <h1 class="mt-1 text-2xl sm:text-[28px] font-semibold tracking-[-0.02em] text-ink-900 dark:text-ink-100">
        Nueva <span class="font-serif font-normal text-primary-600">pregunta</span>
    </h1>
    <p class="text-ink-500 text-sm mt-1">Registra una pregunta y respuesta para alimentar el chatbot.</p>
</section>

<form asp-action="Create" method="post" class="space-y-5">
    <div asp-validation-summary="ModelOnly"
         class="text-sm text-rose-700 bg-rose-50 ring-1 ring-rose-200 p-3 rounded-md"></div>

    <partial name="~/Pages/Admin/InfoManagement/Faq/_Form.cshtml" model="Model" />

    <div class="flex flex-col-reverse sm:flex-row sm:justify-end gap-3 pt-2">
        <a asp-action="Index"
           class="inline-flex items-center justify-center gap-2 px-5 py-2.5 rounded-md bg-white dark:bg-ink-800 ring-soft text-ink-700 dark:text-ink-200 text-sm font-semibold hover:bg-ink-100 dark:hover:bg-ink-700 transition-all">
            <i class="ti ti-arrow-left text-xs"></i> Cancelar
        </a>
        <button type="submit"
                class="btn-grad inline-flex items-center justify-center gap-2 px-5 py-2.5 rounded-md text-sm font-semibold hover:-translate-y-0.5 transition-all">
            <i class="ti ti-device-floppy text-xs"></i> Guardar pregunta
        </button>
    </div>
</form>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
